import { RoleTitlePipe } from './role-title.pipe';

describe('RoleTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
