import { ImportStatePipe } from './import-state.pipe';

describe('ImportStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ImportStatePipe();
    expect(pipe).toBeTruthy();
  });
});
