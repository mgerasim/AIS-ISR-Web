import { FileExtensionIconPipe } from './file-extension-icon.pipe';

describe('FileExtensionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtensionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
