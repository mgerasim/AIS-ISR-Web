import { ExaminationStatusPipe } from './examination-status.pipe';

describe('ExaminationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ExaminationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
